
1、使用asc2bcdx时需要注意填充符号的选择。
	如果输入的asc字符串长度是奇数，那么使用
	bcd2ascx转换成asc码时会输出偶长度的asc码，
	若没有使用前填充，则最后一个是填充符号。
	若使用了前填充，则第一个字符是填充符号。
	如果使用了全填充，则填充符号会被截取，不管输入的
	asc字符串是否有有填充符号。
	
	最好不要在输入的asc字符串中包含填充符号。